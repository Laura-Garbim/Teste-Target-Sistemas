function inverterString(str) {
    let resultado = '';
    for (let i = str.length - 1; i >= 0; i--) {
        resultado += str[i];
    }
    return resultado;
}

const entrada = prompt('Digite a string que deseja inverter:');
const invertida = inverterString(entrada);

console.log(`String original: ${entrada}`);
console.log(`String invertida: ${invertida}`);
